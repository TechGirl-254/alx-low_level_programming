#include "main.h"

/**
 * _islower- Checks for lowercase character c
 *
 * @c: a character arguement
 *
 * Return: Always 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
	_putchar('\n');
}
