#include "main.h"

/**
 * mul - Multiplies two integers
 *
 * @a: One of the integers multipled by the function
 *
 * @b: One of the integers multiplied by the function
 *
 * Return: Always(0)
 */
int mul(int a, int b)
{
	return (a * b);
}
