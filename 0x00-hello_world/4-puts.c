#include <stdio.h>

/**
 * This program prints a text on stdout
 *
 * Main -Entry point
 *
 * Return Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
